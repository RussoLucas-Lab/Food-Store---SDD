# Ejemplo de variables de entorno para backend Food Store
# Nunca subir .env real, solo .env.example

DB_HOST=localhost
DB_PORT=5432
DB_USER=devuser
DB_PASS=devpass
DB_NAME=foodstore_dev
SECRET_KEY=changeme
ENV=development
