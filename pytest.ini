[pytest]
testpaths = backend/tests
addopts = --cov=backend --cov-report=html --cov-report=term-missing
markers =
    unit: unit tests with mocked repositories
    integration: integration tests via HTTP TestClient

[coverage:run]
omit = backend/tests/*
