/**
 * Shared Components - Atomic design system
 *
 * Components are organized by complexity level:
 * - Atoms: Basic UI elements (Button, Input, Label, Text)
 * - Molecules: Combinations of atoms (FormField, Card, Spinner, Messages)
 * - Organisms: Complex layouts (Layout, Header, Modal, Sidebar)
 *
 * Usage:
 * import { Button, Input } from '@/shared/components/atoms';
 * import { FormField, Card } from '@/shared/components/molecules';
 * import { Layout, Modal } from '@/shared/components/organisms';
 */

export * from './atoms';
export * from './molecules';
export * from './organisms';
