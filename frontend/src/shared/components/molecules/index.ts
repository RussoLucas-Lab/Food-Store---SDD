/**
 * Molecular Components - Compositions of atoms
 *
 * Molecules are composed of atoms and have minimal business logic.
 * They can be used in pages and features.
 *
 * Usage:
 * import { FormField, Card, LoadingSpinner, ErrorMessage, SuccessMessage } from '@/shared/components/molecules';
 */

export {
  FormField,
  Card,
  LoadingSpinner,
  ErrorMessage,
  SuccessMessage,
} from './FormField';
export type { FormFieldProps, CardProps } from './FormField';
