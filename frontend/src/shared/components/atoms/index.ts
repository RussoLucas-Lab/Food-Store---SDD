/**
 * Atomic Components - Basic building blocks
 *
 * Atoms are the simplest components with no business logic dependencies.
 * They are purely presentational and can be used everywhere.
 *
 * Usage:
 * import { Button, Input, Label, Text } from '@/shared/components/atoms';
 */

export { Button } from './Button';
export type { ButtonProps } from './Button';

export { Input } from './Input';
export type { InputProps } from './Input';

export { Label, Text } from './Label';
export type { LabelProps, TextProps } from './Label';
